#!/bin/python3

import math
import os
import random
import re
import sys
import argparse

    wordList = [0] * numWords
    for y in range(numWords):
        for x in range (5):
            diceNumber = str(random.randrange(1,5)) + diceNumber
        # wordList[y-1] = diceNumber
        print(diceNumber)
        wordList[y] = diceNumber
        diceNumber = ''
